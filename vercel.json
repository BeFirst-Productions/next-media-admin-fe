{
  "installCommand": "npm install --ignore-scripts",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { 
      "source": "/api/(.*)", 
      "destination": "https://api.nextmedia.ae/api/$1" 
    },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
